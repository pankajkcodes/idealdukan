import 'package:flutter/material.dart';

class CartFrgamnet extends StatelessWidget {
  const CartFrgamnet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
